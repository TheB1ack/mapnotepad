<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com" 
             xmlns:zxing="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MapNotepad.Views.QrScannerPage"
             xmlns:resources="clr-namespace:MapNotepad.Resources"
             Title="{x:Static resources:Resource.QrScanTitle}">
    
    <Grid>
        
        <zxing:ZXingScannerView IsScanning="True"
                                Result="{Binding ScanResult, Mode=TwoWay}"
                                ScanResultCommand="{Binding ScanResultCommand}"/>

        <zxing:ZXingDefaultOverlay TopText="{x:Static resources:Resource.QrScanHint}"/>

    </Grid>
    
</ContentPage>