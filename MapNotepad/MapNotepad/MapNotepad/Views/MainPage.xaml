<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:local="clr-namespace:MapNotepad.Views"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
             xmlns:behaviors="clr-namespace:MapNotepad.Behaviors"
             xmlns:resources="clr-namespace:MapNotepad.Resources"
             android:TabbedPage.ToolbarPlacement="Bottom"
             x:Class="MapNotepad.Views.MainPage"
             Title="{x:Static resources:Resource.MapNotepanTitle}">
    <TabbedPage.ToolbarItems>
        <ToolbarItem   
                 Command="{Binding LogOutClickCommand}"
                 Text="{x:Static resources:Resource.LogoutButton}"/>
        
        <ToolbarItem   
                 Command="{Binding qrClickCommand}"
                 IconImageSource="qrCode.png"/>
       
    </TabbedPage.ToolbarItems>

    <TabbedPage.Children>
        <local:MapPage IconImageSource="Map.png"/>
        <local:PinsListPage IconImageSource="List.png"/>
        
    </TabbedPage.Children>

    <TabbedPage.Behaviors>
        <behaviors:TabbedPageNavigationBehavior />

    </TabbedPage.Behaviors>
    
</TabbedPage>